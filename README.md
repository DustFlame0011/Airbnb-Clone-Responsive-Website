🏡 Airbnb Clone – Responsive Website

Tech Stack: HTML • CSS (Pure CSS, no frameworks)

📌 Overview

This project is a simplified Airbnb-style landing page clone built using only HTML and pure CSS. The focus of the project is to practice responsive web design techniques without relying on any CSS frameworks.

🎯 Project Purpose

To strengthen my foundational front-end skills—especially layout, responsiveness, and visual design—by recreating a real-world UI with only core web technologies.

✨ Key Features

Fully Responsive Layout: Adapts smoothly to mobile, tablet, and desktop sizes.

Modern UI Design: Clean layout inspired by Airbnb’s design system.

Custom CSS Grid & Flexbox: No frameworks (Bootstrap/Tailwind) used.

Reusable Components: Card layouts, navigation bar, hero section, and footer built from scratch.

Accessible & Semantic HTML: Clear structure for better readability and SEO.

🛠️ What I Learned

How to structure a webpage using semantic HTML elements.

Designing complex layouts with CSS Grid, Flexbox, and media queries.

Implementing responsive images, spacing systems, and typography.

Improving consistency using CSS variables.

🚀 Future Improvements

Add simple interactivity with JavaScript (carousel, mobile menu).

Expand to a multi-page layout (listings, details page).

Apply a design system with tokens (colors, spacing, typography).
